package org.chathura.Interfaces;

//interfaces can be use to have abstractions
//canonot have a body for methods in the interface
//cannot have private or protected methods inside a interface
public interface CarInterface {

    void start();
    void move(int speed);

}
